<% provide(:title, 'All users') %>
<h1>All users</h1>

<ul class="users">
  <% @users.each do |user| %>
    <li>
		
		<%#= gravatar_for user, size: 50 %>
      	<%= link_to user.email, users_show_path(user.id) %>
    </li>
  <% end %>
  
</ul>
<%= paginate @users %>